import { Modal } from "@gravity-ui/uikit";
import React from "react";
import { AppSettingsProps } from "./types";

const AppSettings: React.FC<AppSettingsProps> = (props: AppSettingsProps) => {
    const defaultColors: number[][] = [
        [255, 230, 230],
        [240, 208, 252],
        [208, 230, 252],
        [252, 249, 208]
    ];
    return (
        <Modal>
        sex
        </Modal>
    );
};

export default AppSettings;